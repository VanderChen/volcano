# Create this after case-01-pg-hard-podgroups.yaml.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ta-deploy-pg-hard-a
  namespace: default
spec:
  replicas: 2
  selector:
    matchLabels:
      app: ta-deploy-pg-hard-a
  template:
    metadata:
      labels:
        app: ta-deploy-pg-hard-a
      annotations:
        scheduling.k8s.io/group-name: ta-deploy-pg-hard-a
    spec:
      schedulerName: volcano
      containers:
      - name: worker
        image: busybox:1.36
        imagePullPolicy: IfNotPresent
        command: ["sh", "-c", "sleep 360000"]
        resources:
          requests:
            cpu: 100m
            memory: 64Mi
