# Create this after case-02-pg-soft-podgroups.yaml.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ta-deploy-pg-soft-a
  namespace: default
spec:
  replicas: 2
  selector:
    matchLabels:
      app: ta-deploy-pg-soft-a
  template:
    metadata:
      labels:
        app: ta-deploy-pg-soft-a
      annotations:
        scheduling.k8s.io/group-name: ta-deploy-pg-soft-a
    spec:
      schedulerName: volcano
      containers:
      - name: worker
        image: busybox:1.36
        imagePullPolicy: IfNotPresent
        command: ["sh", "-c", "sleep 360000"]
        resources:
          requests:
            cpu: 100m
            memory: 64Mi
